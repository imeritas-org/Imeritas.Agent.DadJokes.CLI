# Deployment Guide — Imeritas.Agent.DadJokes.CLI

---

## Prerequisites

- .NET net10.0 SDK
- Imeritas.Agent.Host (running instance)
- GitHub Packages NuGet source configured

---

## Build

```
dotnet build Imeritas.Agent.DadJokes.CLI.slnx
```

## Test

```
dotnet test src/Imeritas.Agent.DadJokes.CLI.Tests
```

## Deploy as Extension

(to be populated — follows framework ExtensionLoader pattern)
